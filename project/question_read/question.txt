다음 중 C 언어에서 정수를 입력받는 함수로 가장 적절한 것은 무엇인가? //하급 문제

printf()
scanf()
gets()
puts()
fopen()
2
다음 중 C 언어에서 올바른 변수 선언은 무엇인가?

int number;
num int;
int 123num;
int_num;
int-num;
1
다음 중 C 언어에서 포인터 변수 선언이 올바른 것은 무엇인가?

ptr int*;
int ptr*;
*int ptr;
int *ptr;
int* ptr();
4
다음 중 C 언어에서 for 반복문의 초기화식으로 올바른 것은?

for(i = 0; i < 10; i++) {}
for(i = 0; i < 10; i--)
for(i = 0; i < 10; i++)
for i = 0; i < 10; i++
for i = 0; i < 10; i--;
1
다음 중 C 언어에서 배열 선언이 올바른 것은?

int arr[5];
int arr();
int arr[5,5];
arr int[5];
int *arr[5];
1
다음 중 C 언어에서 함수 반환형으로 올바른 것은?

void func();
int func();
float func();
double func();
string func();
2
다음 중 C 언어에서 구조체 선언이 올바른 것은?

struct Point { int x; int y; };
struct Point ( int x; int y; );
struct Point { x int; y int; };
struct Point { int x, y };
struct Point { int x; int y }
1
다음 중 C 언어에서 올바른 if 조건식은?

if (x = 5) {}
if (x == 5) {}
if x == 5 {}
if x = 5 {}
if (x != 5) {}
2
다음 중 C 언어에서 malloc 함수의 역할은 무엇인가?

메모리 할당
메모리 해제
파일 열기
입력 받기
출력 하기
1
다음 중 C 언어에서 올바른 문자열 초기화는?

char str[6] = "hello";
char str[] = "hello";
char str[5] = "hello";
char str[6] = {'h','e','l','l','o'};
char str[] = {'h','e','l','l','o', '\0'};
2
다음 중 C 언어에서 배열과 포인터의 차이점으로 올바른 것은?

배열은 고정 크기, 포인터는 가변 크기
배열과 포인터는 동일하다
배열은 동적 메모리, 포인터는 정적 메모리
배열은 함수만 사용, 포인터는 변수만 사용
배열은 지역변수, 포인터는 전역변수
1
다음 중 C 언어에서 switch 문에 올바른 break 위치는?

case 1: break;
case 1: printf("a"); break;
case 1: printf("a");
case 2: break;
case 1: printf("a"); break; default: break;
5
다음 중 C 언어에서 올바른 함수 호출은?

func();
func;
func(void);
func(void);
func{};
1
다음 중 C 언어에서 배열의 인덱스 범위로 올바른 것은?

0 ~ (크기-1)
1 ~ 크기
-1 ~ (크기-1)
0 ~ 크기
-1 ~ 크기
1
다음 중 C 언어에서 올바른 주석 문법은?

// 주석
/* 주석 */
# 주석
<!-- 주석 -->
/* 주석
2
다음 중 C 언어에서 구조체 변수 선언으로 올바른 것은?

struct Point p;
Point p;
struct Point;
p Point;
struct Point p();
1
다음 중 C 언어에서 포인터 연산이 올바른 것은?

ptr++;
ptr--;
ptr = ptr + 1;
ptr = ptr - 1;
위 모두 올바르다
5
다음 중 C 언어에서 함수 매개변수 선언이 올바른 것은?

void func(int a);
void func(int);
void func(int a, int b);
void func(int a, int b, int c);
위 모두 올바르다
5
다음 중 C 언어에서 올바른 메모리 해제 함수는?

free();
malloc();
calloc();
realloc();
memset();
1
다음 중 C 언어에서 올바른 포인터 초기화는?

int *ptr = NULL;
int ptr = NULL;
int *ptr = 0;
int ptr;
int *ptr = *ptr;
1
다음 중 C 언어에서 올바른 배열 복사 방법은?

for (int i=0; i<size; i++) dest[i] = src[i];
memcpy(dest, src, size * sizeof(int));
strcpy(dest, src);
dest = src;
dest == src;
2
다음 중 C 언어에서 올바른 포인터 연산은 무엇인가?

ptr++;
ptr--;
ptr = ptr + 1;
ptr = ptr - 1;
위 모두 올바르다
5
다음 중 C 언어에서 올바른 함수 매개변수 선언은 무엇인가?

void func(int a);
void func(int);
void func(int a, int b);
void func(int a, int b, int c);
위 모두 올바르다
5
다음 중 C 언어에서 올바른 메모리 해제 함수는 무엇인가?

free();
malloc();
calloc();
realloc();
memset();
1
다음 중 C 언어에서 올바른 포인터 초기화는 무엇인가?

int *ptr = NULL;
int ptr = NULL;
int *ptr = 0;
int ptr;
int *ptr = *ptr;
1
다음 중 C 언어에서 올바른 배열 복사 방법은 무엇인가?

for (int i=0; i<size; i++) dest[i] = src[i];
memcpy(dest, src, size * sizeof(int));
strcpy(dest, src);
dest = src;
dest == src;
2
다음 중 C 언어에서 올바른 구조체 멤버 접근법은 무엇인가?

struct Point p; p.x = 10;
struct Point p; p->x = 10;
struct Point p; (*p).x = 10;
struct Point* p; p->x = 10;
struct Point* p; p.x = 10;
1
다음 중 C 언어에서 올바른 문자열 복사 함수는 무엇인가?

strcpy(dest, src);
strncpy(dest, src, n);
memcpy(dest, src, n);
strcat(dest, src);
strtok(dest, src);
1
다음 중 C 언어에서 올바른 포인터 비교 방법은 무엇인가?

if (ptr1 == ptr2) {}
if (ptr1 = ptr2) {}
if (ptr1 != ptr2) {}
if (ptr1 => ptr2) {}
if (ptr1 =! ptr2) {}
1
다음 중 C 언어에서 올바른 2차원 배열 선언은 무엇인가?

int arr[3][4];
int arr[3,4];
int arr[4][3];
int arr[];
int arr[3][];
1
다음 중 C 언어에서 올바른 동적 메모리 할당 함수는 무엇인가?

malloc();
free();
calloc();
realloc();
memset();
1
다음 중 C 언어에서 올바른 메모리 할당 후 초기화 함수는 무엇인가?

calloc();
malloc();
free();
realloc();
memset();
1
다음 중 C 언어에서 올바른 조건 연산자는 무엇인가?

(조건) ? 참일때 : 거짓일때
if (조건) {}
switch (조건) {}
while (조건) {}
for (조건) {}
1
다음 중 C 언어에서 올바른 함수 포인터 선언법은 무엇인가?

int (*funcPtr)(int);
int *funcPtr(int);
int funcPtr*(int);
int funcPtr(int*);
int *funcPtr;
1
다음 중 C 언어에서 올바른 메모리 해제 함수 호출법은 무엇인가?

free(ptr);
malloc(ptr);
calloc(ptr);
realloc(ptr);
memset(ptr);
1
다음 중 C 언어에서 올바른 포인터 역참조는 무엇인가?

*ptr = 10;
ptr* = 10;
ptr = *10;
*ptr = *10;
ptr* = *10;
1
다음 중 C 언어에서 올바른 배열 크기 계산법은 무엇인가?

sizeof(arr) / sizeof(arr[0]);
sizeof(arr);
sizeof(arr[0]);
sizeof(arr) * sizeof(arr[0]);
sizeof(arr) + sizeof(arr[0]);
1
다음 중 C 언어에서 올바른 구조체 선언법은 무엇인가?

struct Person { char name[20]; int age; };
struct Person ( char name[20]; int age; );
struct Person { name char[20]; age int; };
struct Person { char name[20], int age };
struct Person { char name[20]; int age }
1
다음 중 C 언어에서 올바른 메모리 재할당 함수는 무엇인가?

realloc();
malloc();
calloc();
free();
memset();
1
다음 중 C 언어에서 올바른 함수 반환 타입은 무엇인가?

int
void
float
double
모두 해당
5
다음 중 C 언어에서 올바른 문자열 길이 구하는 함수는 무엇인가?

strlen();
strcpy();
strcmp();
strcat();
strdup();
1
다음 중 C 언어에서 올바른 포인터와 배열의 차이점은 무엇인가?

배열은 고정 크기, 포인터는 가변 크기
배열과 포인터는 동일하다
배열은 동적 메모리, 포인터는 정적 메모리
배열은 함수만 사용, 포인터는 변수만 사용
배열은 지역변수, 포인터는 전역변수
1
다음 중 C 언어에서 올바른 함수 선언은 무엇인가?

int sum(int a, int b);
int sum(a, b);
void sum(int a, int b);
int sum int a, int b;
int sum(int, int);
1
다음 중 C 언어에서 올바른 메모리 할당 크기 계산법은 무엇인가?

size * sizeof(int)
size / sizeof(int)
sizeof(int) / size
size + sizeof(int)
size - sizeof(int)
1
다음 중 C 언어에서 올바른 전처리기 지시자는 무엇인가?

#define MAX 100
#DEFINE MAX 100
define MAX 100
#MAX 100
$define MAX 100
1
다음 중 C 언어에서 올바른 파일 열기 모드는 무엇인가?

"r"
"w"
"a"
"r+"
모두 해당
5
다음 중 C 언어에서 올바른 구조체 포인터 멤버 접근법은 무엇인가?

struct Point* p; p->x = 10;
struct Point* p; (*p).x = 10;
struct Point* p; p.x = 10;
struct Point p; p->x = 10;
struct Point p; (*p).x = 10;
1
다음 중 C 언어에서 올바른 switch 문의 default 위치는?

case문들 뒤에 위치
case문들 앞에 위치
처음에 위치
마지막에 위치하거나 아무 위치나 가능
default는 없어도 됨
4
다음 중 C 언어에서 올바른 포인터 산술 연산은 무엇인가?

ptr + 1
ptr - 1
ptr++
ptr--
위 모두 올바르다
5
다음 중 C 언어에서 올바른 함수 선언과 정의는 무엇인가?

int func(int a) { return a + 1; }
void func(int a) { return a; }
int func { return 0; }
func int(int a) { return a; }
int func(int a);
1
다음 중 C 언어에서 올바른 변수 스코프(scope)는 무엇인가?

지역 변수는 함수 내부에서만 사용 가능
전역 변수는 모든 함수에서 사용 가능
정적 변수는 선언된 함수 내부에서만 유지됨
정적 변수는 초기화 시 한 번만 실행됨
위 모두 맞음
5
다음 중 C 언어에서 올바른 malloc 사용법은 무엇인가?

int *ptr = (int*)malloc(sizeof(int) * n);
int *ptr = malloc(n);
int *ptr = malloc(sizeof(int));
int *ptr = malloc(sizeof(int) * n);
int *ptr = (int*)malloc(n);
4
다음 중 C 언어에서 올바른 포인터 타입 변환은 무엇인가?

void* ptr = malloc(sizeof(int));
int* iptr = (int*)ptr;
char* cptr = (char*)ptr;
float* fptr = (float*)ptr;
위 모두 가능
5
다음 중 C 언어에서 올바른 반복문 종료 조건은 무엇인가?

while(i < 10) { i++; }
for(int i = 0; i < 10; i++) {}
do { i++; } while(i < 10);
for(;;) { break; }
위 모두 맞음
5
다음 중 C 언어에서 올바른 enum 선언은 무엇인가?

enum Color { RED, GREEN, BLUE };
enum Color { RED = 1, GREEN = 2, BLUE = 3 };
enum Color { RED, GREEN=5, BLUE };
enum Color { RED=0, GREEN, BLUE };
위 모두 맞음
5
다음 중 C 언어에서 올바른 문자열 비교 함수는 무엇인가?

strcmp(str1, str2);
strncmp(str1, str2, n);
strcasecmp(str1, str2);
strncasecmp(str1, str2, n);
위 모두 맞음
5
다음 중 C 언어에서 올바른 함수 포인터 선언은 무엇인가?

int (*funcPtr)(int);
int *funcPtr(int);
int funcPtr*(int);
int funcPtr(int*);
int *funcPtr;
1
다음 중 C 언어에서 올바른 배열 초기화는 무엇인가?

int arr[5] = {1, 2, 3, 4, 5};
int arr[] = {1, 2, 3, 4, 5};
int arr[5] = {0};
int arr[5] = {};
int arr[5] = {1, 2, 3};
3
다음 중 C 언어에서 올바른 포인터 역참조 방법은 무엇인가?

*ptr = 10;
ptr* = 10;
ptr = *10;
*ptr = *10;
ptr* = *10;
1
다음 중 C 언어에서 올바른 동적 메모리 해제 방법은 무엇인가?

free(ptr);
malloc(ptr);
calloc(ptr);
realloc(ptr);
memset(ptr);
1
다음 중 C 언어에서 올바른 함수 반환형 키워드는 무엇인가?

int
void
float
double
모두 맞음
5
다음 중 C 언어에서 올바른 변수 초기화 방법은 무엇인가?

int x = 10;
int x;
int x =;
int = 10;
int x == 10;
1
다음 중 C 언어에서 올바른 배열 선언은 무엇인가?

int arr[10];
int arr();
int arr[];
int arr[10, 10];
int arr[10][10];
5
다음 중 C 언어에서 올바른 포인터 선언은 무엇인가?

int *ptr;
ptr int*;
int ptr*;
*int ptr;
int* ptr();
1
다음 중 C 언어에서 올바른 함수 선언은 무엇인가?

void func(void);
int func(int a, int b);
float func();
char func(char c);
모두 맞음
5
다음 중 C 언어에서 올바른 조건문 작성은 무엇인가?

if (x == 10) {}
if x == 10 {}
if (x = 10) {}
if x = 10 {}
if (x != 10) {}
1
다음 중 C 언어에서 올바른 구조체 선언법은 무엇인가?

struct Person { char name[20]; int age; };
struct Person ( char name[20]; int age; );
struct Person { name char[20]; age int; };
struct Person { char name[20], int age };
struct Person { char name[20]; int age }
1
다음 중 C 언어에서 올바른 포인터 연산은 무엇인가?

ptr++;
ptr--;
ptr = ptr + 1;
ptr = ptr - 1;
위 모두 맞음
5
다음 중 C 언어에서 올바른 메모리 할당 함수는 무엇인가?

malloc();
free();
calloc();
realloc();
memset();
1
다음 중 C 언어에서 올바른 포인터 역참조 방법은 무엇인가?

*ptr = 10;
ptr* = 10;
ptr = *10;
*ptr = *10;
ptr* = *10;
1
다음 중 C 언어에서 올바른 함수 포인터 선언은 무엇인가?

int (*funcPtr)(int);
int *funcPtr(int);
int funcPtr*(int);
int funcPtr(int*);
int *funcPtr;
1
다음 중 C 언어에서 올바른 문자열 복사 함수는 무엇인가?

strcpy(dest, src);
strncpy(dest, src, n);
memcpy(dest, src, n);
strcat(dest, src);
strtok(dest, src);
1
다음 중 C 언어에서 올바른 배열 크기 계산법은 무엇인가?

sizeof(arr) / sizeof(arr[0]);
sizeof(arr);
sizeof(arr[0]);
sizeof(arr) * sizeof(arr[0]);
sizeof(arr) + sizeof(arr[0]);
1
다음 중 C 언어에서 올바른 문자열 길이 구하는 함수는 무엇인가?

strlen();
strcpy();
strcmp();
strcat();
strdup();
1
다음 중 C 언어에서 올바른 포인터 초기화는 무엇인가?

int *ptr = NULL;
int ptr = NULL;
int *ptr = 0;
int ptr;
int *ptr = *ptr;
1
다음 중 C 언어에서 올바른 메모리 해제 함수는 무엇인가?

free();
malloc();
calloc();
realloc();
memset();
1
다음 중 C 언어에서 올바른 함수 반환형 키워드는 무엇인가?

int
void
float
double
모두 맞음
5
다음 중 C 언어에서 올바른 변수 스코프는 무엇인가?

지역 변수는 함수 내부에서만 사용 가능
전역 변수는 모든 함수에서 사용 가능
정적 변수는 선언된 함수 내부에서만 유지됨
정적 변수는 초기화 시 한 번만 실행됨
위 모두 맞음
5
다음 중 C 언어에서 올바른 반복문 종료 조건은 무엇인가?

while(i < 10) { i++; }
for(int i = 0; i < 10; i++) {}
do { i++; } while(i < 10);
for(;;) { break; }
위 모두 맞음
5
다음 중 C 언어에서 올바른 전처리기 지시자는 무엇인가?

#define MAX 100
#DEFINE MAX 100
define MAX 100
#MAX 100
$define MAX 100
1
다음 중 C 언어에서 올바른 파일 열기 모드는 무엇인가?

"r"
"w"
"a"
"r+"
모두 맞음
5
다음 중 C 언어에서 올바른 포인터 산술 연산은 무엇인가?

ptr + 1
ptr - 1
ptr++
ptr--
위 모두 맞음
5
다음 중 C 언어에서 올바른 함수 선언과 정의는 무엇인가?

int func(int a) { return a + 1; }
void func(int a) { return a; }
int func { return 0; }
func int(int a) { return a; }
int func(int a);
1
다음 중 C 언어에서 올바른 enum 선언은 무엇인가?

enum Color { RED, GREEN, BLUE };
enum Color { RED = 1, GREEN = 2, BLUE = 3 };
enum Color { RED, GREEN=5, BLUE };
enum Color { RED=0, GREEN, BLUE };
위 모두 맞음
5
다음 중 C 언어에서 올바른 문자열 비교 함수는 무엇인가?

strcmp(str1, str2);
strncmp(str1, str2, n);
strcasecmp(str1, str2);
strncasecmp(str1, str2, n);
위 모두 맞음
5
다음 중 C 언어에서 올바른 메모리 재할당 함수는 무엇인가?

realloc();
malloc();
calloc();
free();
memset();
1
다음 중 C 언어에서 올바른 배열 초기화는 무엇인가?

int arr[5] = {1, 2, 3, 4, 5};
int arr[] = {1, 2, 3, 4, 5};
int arr[5] = {0};
int arr[5] = {};
int arr[5] = {1, 2, 3};
3
다음 중 C 언어에서 올바른 함수 매개변수 선언은 무엇인가?

void func(int a);
void func(int);
void func(int a, int b);
void func(int a, int b, int c);
위 모두 맞음
5
다음 중 C 언어에서 올바른 포인터 타입 변환은 무엇인가?

void* ptr = malloc(sizeof(int));
int* iptr = (int*)ptr;
char* cptr = (char*)ptr;
float* fptr = (float*)ptr;
위 모두 가능
5
다음 중 C 언어에서 올바른 구조체 멤버 접근법은 무엇인가?

struct Point p; p.x = 10;
struct Point p; p->x = 10;
struct Point p; (*p).x = 10;
struct Point* p; p->x = 10;
struct Point* p; p.x = 10;
1
다음 중 C 언어에서 올바른 메모리 할당 크기 계산법은 무엇인가?

size * sizeof(int)
size / sizeof(int)
sizeof(int) / size
size + sizeof(int)
size - sizeof(int)
1
다음 중 C 언어에서 올바른 조건 연산자는 무엇인가?

(조건) ? 참일때 : 거짓일때
if (조건) {}
switch (조건) {}
while (조건) {}
for (조건) {}
1
다음 중 C 언어에서 올바른 변수 초기화 방법은 무엇인가?

int x = 10;
int x;
int x =;
int = 10;
int x == 10;
1
다음 중 C 언어에서 올바른 함수 반환 타입은 무엇인가?

int
void
float
double
모두 맞음
5
다음 중 C 언어에서 올바른 포인터 초기화는 무엇인가?

int *ptr = NULL;
int ptr = NULL;
int *ptr = 0;
int ptr;
int *ptr = *ptr;
1
다음 중 C 언어에서 올바른 동적 메모리 해제 방법은 무엇인가?

free(ptr);
malloc(ptr);
calloc(ptr);
realloc(ptr);
memset(ptr);
1
다음 중 C 언어에서 올바른 변수 스코프(scope)는 무엇인가?

지역 변수는 함수 내부에서만 사용 가능
전역 변수는 모든 함수에서 사용 가능
정적 변수는 선언된 함수 내부에서만 유지됨
정적 변수는 초기화 시 한 번만 실행됨
위 모두 맞음
5
다음 중 C 언어에서 올바른 반복문 종료 조건은 무엇인가?

while(i < 10) { i++; }
for(int i = 0; i < 10; i++) {}
do { i++; } while(i < 10);
for(;;) { break; }
위 모두 맞음
5
다음 중 C 언어에서 올바른 메모리 해제 함수 호출법은 무엇인가?

free(ptr);
malloc(ptr);
calloc(ptr);
realloc(ptr);
memset(ptr);
1
다음 중 C 언어에서 올바른 전처리기 지시자는 무엇인가?

#define MAX 100
#DEFINE MAX 100
define MAX 100
#MAX 100
$define MAX 100
1
다음 중 C 언어에서 올바른 파일 열기 모드는 무엇인가?

"r"
"w"
"a"
"r+"
모두 맞음
5
다음 중 C 언어에서 포인터 변수 선언이 올바른 것은 무엇인가?

int val;
int* ptr();
float ptr*;
char ptr;
ptr double;
4
다음 중 C 언어에서 정수형 변수를 올바르게 선언한 것은 무엇인가?

int 1num;
integer num;
int num;
num int;
int = num;
3
다음 중 C 언어에서 조건문에 해당하는 키워드는 무엇인가?

switch
if
loop
function
case
2
다음 중 C 언어에서 문자 하나를 저장하기에 적절한 자료형은 무엇인가?

int
float
char
double
long
3
다음 중 C 언어에서 배열을 선언하는 방법으로 올바른 것은 무엇인가?

int array;
int array(10);
int array[];
int array[10];
int[10] array;
4
다음 중 C 언어에서 무한 루프를 만드는 코드로 올바른 것은 무엇인가?

for (i = 0; i < 10; i--)
while (1)
if (1)
do while (1)
repeat until (0)
2
다음 중 C 언어에서 포인터 변수 선언이 올바른 것은 무엇인가?

int ptr;
int* ptr();
*int ptr;
int *ptr;
ptr int*;
4
다음 중 C 언어에서 포인터 변수 선언이 올바른 것은 무엇인가?

int ptr;
int* ptr();
*int ptr;
int *ptr;
ptr int*;
4
다음 중 printf() 함수에서 정수 출력 형식 지정자로 올바른 것은 무엇인가?

%c
%s
%d
%f
%lf
3
다음 중 C 언어의 논리 연산자가 아닌 것은 무엇인가?

&&
||
!
==
!=
4
다음 중 C 언어에서 파일을 쓰기 모드로 여는 함수 호출은 무엇인가?

fopen("file.txt", "r")
fopen("file.txt", "w")
fopen("file.txt", "rw")
fwrite("file.txt")
fread("file.txt")
2
다음 중 C 언어에서 문자 상수를 표현하는 방법은?

"a"
'a'
a
"abc"
'abc'
2
다음 중 C 언어에서 부울(Boolean) 값을 나타내는 자료형은?

bool
boolean
int
char
void
3
다음 중 C 언어에서 주석을 작성하는 올바른 방법은?

// 주석 내용
/* 주석 내용 */
# 주석 내용
<!-- 주석 내용 -->
A, B 모두 맞음
5
다음 중 변수의 초기화가 올바른 것은?

int num = 10;
int num; = 10
int num 10;
int = 10 num;
num int = 10;
1
다음 중 배열을 함수의 인자로 전달할 때 발생하는 현상으로 올바른 설명은? //중급

배열 전체가 복사된다.
배열은 포인터로 전달된다.
배열은 전역변수가 된다.
배열 크기를 반드시 전달해야 한다.
배열 인자는 사용할 수 없다.
2
다음 중 C 언어에서 동적 메모리 할당 시 사용하는 함수는 무엇인가?

malloc()
free()
printf()
scanf()
exit()
1
다음 중 C 언어에서 포인터를 이용해 변수 값을 변경하는 올바른 방법은?

*ptr = 10;
ptr = 10;
ptr->value = 10;
ptr = &10;
ptr == 10;
1
다음 중 C 언어에서 올바른 구조체 선언 방법은?

struct Person { char name[20]; int age; };
struct Person ( char name[20]; int age; );
struct Person { name char[20]; age int; };
struct Person { char name[20], int age; };
struct Person { char name[20]; int age }
1
다음 중 C 언어에서 포인터 배열과 이중 포인터의 차이는 무엇인가?

포인터 배열은 여러 포인터를 저장, 이중 포인터는 포인터를 가리킴
포인터 배열은 포인터 하나, 이중 포인터는 두 개
둘은 동일하다
포인터 배열은 값만, 이중 포인터는 주소만 저장
포인터 배열은 함수만, 이중 포인터는 변수만 사용
1
다음 중 C 언어에서 함수 포인터 선언 방법으로 올바른 것은?

int (*funcPtr)(int);
int *funcPtr(int);
int funcPtr*(int);
int funcPtr(int*);
int *funcPtr;
1
다음 중 C 언어에서 배열과 포인터의 차이가 아닌 것은?

배열은 고정 크기, 포인터는 가변 크기
배열은 메모리 주소를 가짐, 포인터는 값 저장
배열은 선언 시 크기 필요, 포인터는 동적 할당 가능
포인터는 산술 연산 가능, 배열은 불가능
배열 이름은 상수 포인터처럼 작동
4
다음 중 C 언어에서 올바른 메모리 해제 방법은?

free(ptr);
malloc(ptr);
calloc(ptr);
realloc(ptr);
memset(ptr);
1
다음 중 C 언어에서 포인터가 NULL인지 확인하는 올바른 방법은?

if (ptr == NULL)
if (ptr = NULL)
if (ptr != NULL)
if (!ptr)
if (ptr = 0)
1
다음 중 C 언어에서 올바른 구조체 멤버 접근 연산자는?

.
->
*.
->
.*
1
다음 중 C 언어에서 올바른 문자열 복사 함수는 무엇인가?

strcpy(dest, src);
strncpy(dest, src, n);
memcpy(dest, src, n);
strcat(dest, src);
strtok(dest, src);
1
다음 중 C 언어에서 포인터와 배열이 함수에 전달될 때 차이는?

포인터는 주소 전달, 배열은 복사 전달
배열은 주소 전달, 포인터는 값 복사
포인터와 배열은 동일하게 주소 전달
배열은 깊은 복사, 포인터는 얕은 복사
둘 다 복사 전달
3
다음 중 C 언어에서 동적 메모리 할당 후 초기화하는 함수는?

calloc()
malloc()
realloc()
free()
memset()
1
다음 중 C 언어에서 배열의 크기를 구하는 올바른 방법은?

sizeof(arr)/sizeof(arr[0])
sizeof(arr)
sizeof(arr[0])
sizeof(arr) * sizeof(arr[0])
sizeof(arr) + sizeof(arr[0])
1
다음 중 C 언어에서 올바른 enum 선언은?

enum Color { RED, GREEN, BLUE };
enum Color { RED = 1, GREEN = 2, BLUE = 3 };
enum Color { RED, GREEN=5, BLUE };
enum Color { RED=0, GREEN, BLUE };
모두 맞음
5
다음 중 C 언어에서 함수 포인터를 통해 함수 호출하는 올바른 방법은?

(*funcPtr)(arg);
funcPtr(arg);
funcPtr->arg;
(*funcPtr).arg;
funcPtr.arg();
2
다음 중 C 언어에서 포인터 연산이 올바른 것은?

ptr++;
ptr--;
ptr = ptr + 1;
ptr = ptr - 1;
모두 맞음
5
다음 중 C 언어에서 올바른 재귀 함수 선언은?

int factorial(int n) { if(n==0) return 1; else return n*factorial(n-1); }
void factorial(int n) { if(n==0) return 1; else return n*factorial(n-1); }
int factorial(int n) { return n*factorial(n-1); }
factorial(int n) { return n*factorial(n-1); }
int factorial(n) { return n*factorial(n-1); }
1
다음 중 C 언어에서 올바른 매크로 상수 선언은?

#define PI 3.14
#define pi 3.14
#define Pi 3.14
#define 3.14 PI
#define PI 3,14
1
다음 중 C 언어에서 올바른 파일 입출력 함수는?

fopen()
fclose()
fread()
fwrite()
모두 맞음
5
다음 중 C 언어에서 올바른 포인터 타입 캐스팅은?

(int*)ptr
(char*)ptr
(float*)ptr
(void*)ptr
모두 맞음
5
다음 중 C 언어에서 올바른 포인터 비교 방법은?

if(ptr1 == ptr2)
if(ptr1 != ptr2)
if(ptr1 > ptr2)
if(ptr1 < ptr2)
모두 가능
5
다음 중 C 언어에서 올바른 배열 선언은?

int arr[10];
int arr[];
int arr[10,10];
int arr(10);
int arr[10][10];
5
다음 중 C 언어에서 올바른 다차원 배열 초기화 방법은?

int arr[2][2] = {{1,2},{3,4}};
int arr[2][2] = {1,2,3,4};
int arr[2][2] = {1,2,3};
int arr[2][2] = {};
int arr[2][2] = {0};
1
다음 중 C 언어에서 올바른 typedef 선언은?

typedef unsigned int uint;
typedef unsigned int;
typedef uint unsigned int;
typedef unsigned int uint
typedef unsigned int uint;
1
다음 중 C 언어에서 올바른 구조체 포인터 멤버 접근법은?

struct Point* p; p->x = 10;
struct Point* p; (*p).x = 10;
struct Point* p; p.x = 10;
struct Point p; p->x = 10;
struct Point p; (*p).x = 10;
1
다음 중 C 언어에서 올바른 문자열 길이 구하는 함수는?

strlen();
strcpy();
strcmp();
strcat();
strdup();
1
다음 중 C 언어에서 올바른 포인터 역참조 방법은?

*ptr = 10;
ptr* = 10;
ptr = *10;
*ptr = *10;
ptr* = *10;
1
다음 중 C 언어에서 올바른 변수 초기화는?

int x = 10;
int x;
int x =;
int = 10;
int x == 10;
1
다음 중 C 언어에서 올바른 함수 반환형 키워드는?

int
void
float
double
모두 맞음
5
다음 중 C 언어에서 올바른 포인터 배열 선언은?

int *arr[10];
int arr*[10];
int *arr();
int *arr[10];
int arr[10]*;
1
다음 중 C 언어에서 올바른 메모리 재할당 함수는?

realloc();
malloc();
calloc();
free();
memset();
1
다음 중 C 언어에서 올바른 변수 스코프는?

지역 변수는 함수 내부에서만 사용 가능
전역 변수는 모든 함수에서 사용 가능
정적 변수는 선언된 함수 내부에서만 유지됨
정적 변수는 초기화 시 한 번만 실행됨
위 모두 맞음
5
다음 중 C 언어에서 올바른 매크로 함수 선언은?

#define MAX(a,b) ((a) > (b) ? (a) : (b))
#define MAX(a,b) (a > b ? a : b)
#define MAX(a,b) a > b ? a : b
#define MAX(a,b) a > b ? (a) : (b)
#define MAX(a,b) ((a > b) ? a : b)
1
다음 중 C 언어에서 올바른 전처리기 매크로 조건 컴파일은?

#ifdef DEBUG
#endif
#if defined(DEBUG)
#endif
모두 맞음
5
다음 중 C 언어에서 올바른 typedef와 struct 사용은?

typedef struct { int x; int y; } Point;
typedef struct Point { int x; int y; } Point;
typedef struct { int x; int y; } point;
typedef struct point { int x; int y; } point;
모두 맞음
5
다음 중 C 언어에서 올바른 동적 메모리 해제는?

free(ptr);
malloc(ptr);
calloc(ptr);
realloc(ptr);
memset(ptr);
1
다음 중 C 언어에서 올바른 파일 입출력 함수는?

fopen()
fclose()
fread()
fwrite()
모두 맞음
5
다음 중 C 언어에서 올바른 매크로 상수 선언은?

#define MAX_SIZE 100
#define max_size 100
#define Max_Size 100
#define 100 MAX_SIZE
#define MAX SIZE 100
1
다음 중 C 언어에서 올바른 문자열 함수는?

strlen()
strcpy()
strcmp()
strcat()
모두 맞음
5
다음 중 C 언어에서 올바른 포인터 타입 변환은?

(void*)ptr
(int*)ptr
(char*)ptr
(float*)ptr
모두 맞음
5
다음 중 C 언어에서 올바른 구조체 포인터 멤버 접근은?

p->member
(*p).member
p.member
(*p)->member
모두 맞음
2
다음 중 C 언어에서 올바른 다차원 배열 선언은?

int arr[3][4];
int arr[3,4];
int arr[3][];
int arr[][4];
모두 맞음
4
다음 중 C 언어에서 올바른 함수 매개변수 선언은?

void func(int a);
void func(int);
void func(int a, int b);
void func(int a, int b, int c);
모두 맞음
5
다음 중 C 언어에서 올바른 변수 선언과 초기화는?

int x = 0;
int y;
int z = 10;
float f = 3.14;
모두 맞음
5
다음 중 C 언어에서 올바른 문자열 상수 선언은?

char *str = "Hello";
char str[] = "Hello";
char str[6] = "Hello";
char str[5] = "Hello";
모두 맞음
4
다음 중 C 언어에서 올바른 전처리기 매크로 함수 선언은?

#define SQUARE(x) ((x) * (x))
#define SQUARE(x) (x * x)
#define SQUARE(x) x * x
#define SQUARE(x) ((x) x)
#define SQUARE(x) (x * (x))
1
다음 중 C 언어에서 올바른 메모리 할당 후 널 포인터 체크는?

if(ptr == NULL)
if(ptr != NULL)
if(!ptr)
if(ptr = NULL)
모두 맞음
4
다음 중 C 언어에서 올바른 포인터 산술 연산은?

ptr + 1
ptr - 1
ptr++
ptr--
모두 맞음
5
다음 중 C 언어에서 올바른 switch-case 문 예외 처리는?

default:
break;
case 0:
break;
case 1:
1
다음 중 C 언어에서 올바른 반복문 탈출 키워드는?

break
continue
goto
return
모두 맞음
5
다음 중 C 언어에서 올바른 다중 포인터 선언은?

int **ptr;
int *ptr*;
int ptr**;
int ** ptr;
모두 맞음
4
다음 중 C 언어에서 올바른 변수 타입 변환은?

(int)3.14
(float)3
(double)5
(char)65
모두 맞음
5
다음 중 malloc() 함수에 대한 설명으로 옳지 않은 것은?

동적 메모리를 할당한다.
반환값은 void 포인터이다.
할당된 메모리는 자동으로 해제된다.
stdlib.h 헤더 파일이 필요하다.
반환값을 형 변환하여 사용하는 것이 일반적이다.
3
다음 중 문자열을 비교하는 함수로 가장 적절한 것은?

strcpy()
strcat()
strcmp()
strlen()
scanf()
3
다음 중 함수 포인터에 대한 선언으로 올바른 것은?

int ptr(int*);
int *ptr(int);
int (*ptr)(int);
(*int ptr)(int);
int ptr(*)(int);
3
다음 중 포인터를 사용하여 배열의 각 요소에 접근하는 코드로 올바른 것은?

arr = arr + 1;
*(arr + i) = 10;
arr(i) = 10;
*arr[i] = 10;
arr -> i = 10;
2
다음 중 C 언어에서 포인터와 배열의 관계에 대한 설명으로 올바른 것은?

배열은 포인터가 아니다.
포인터는 배열의 크기를 자동으로 추적한다.
배열 이름은 배열의 첫 번째 요소의 주소이다.
포인터는 배열보다 항상 느리다.
배열은 포인터와 완전히 동일한 개념이다.
3
다음 중 free() 함수에 대한 설명으로 틀린 것은?

malloc 또는 calloc으로 할당된 메모리를 해제한다.
포인터가 가리키는 메모리를 해제한다.
해제 후 해당 포인터는 자동으로 NULL이 된다.
stdlib.h에 선언되어 있다.
메모리 누수를 방지하기 위해 사용된다.
3
다음 중 함수 포인터를 선언하고 사용하는 올바른 예는?//상급

int ptr(*f)(int);
int (*f)(int) = &func;
int *f(int) = &func;
func = (*int)(f);
int f = (*func)(int);
2
다음 중 2차원 배열을 함수에 전달하는 방법으로 올바른 것은?

void func(int arr[][])
void func(int *arr[])
void func(int arr[][10])
void func(int **arr)
void func(arr[10][10])
3
다음 중 포인터 산술 연산으로 잘못된 것은?

ptr + 1
ptr - 1
ptr++
ptr = ptr + 4.5
*(ptr + 2)
4
다음 중 포인터 배열 선언으로 올바른 것은?

int *arr();
int arr(*)[];
int *arr[5];
int (*arr)[5];
int arr[5]*;
3
다음 중 동적 2차원 배열 할당으로 올바른 코드는?

int **arr = malloc(sizeof(int*));
int arr = malloc(5 * 5);
int **arr = malloc(5 * sizeof(int*));
int *arr[5] = malloc(sizeof(int));
int *arr = malloc(5)[5];
3
다음 중 구조체 포인터에서 멤버 접근이 올바른 것은?

(*ptr).x
*ptr.x
ptr(x)
ptr->*x
ptr.x
1
다음 중 재귀 함수의 기본 구성에 반드시 포함되어야 하는 것은?

반복문
파일 입출력
종료 조건
switch 문
포인터 연산
3
다음 중 const int *p;의 의미는 무엇인가?

p는 상수를 가리킨다.
p는 값을 변경할 수 있다.
*p는 수정 가능하다.
p와 *p 모두 상수이다.
*p는 다른 주소를 가리킨다.
1
다음 중 함수 포인터 배열 선언으로 올바른 것은?

int *fptr[](int);
int (*fptr[3])(int);
int *(*fptr[3])(int);
int fptr[3]*();
int *fptr[3]();
2
다음 중 문자열 길이를 구하는 함수는?

strcpy()
strcat()
strlen()
strcmp()
malloc()
3
다음 중 문자열 길이를 구하는 함수는?

strcpy()
strcat()
strlen()
strcmp()
malloc()
3
다음 코드의 결과는?
int a = 5; printf("%d", a++ + ++a);
10
11
미정 (정의되지 않음)
12
컴파일 에러
3
다음 중 동적 메모리 누수를 방지하는 방법으로 옳은 것은?

free()를 호출하지 않는다.
메모리 주소를 초기화하지 않는다.
malloc() 후 free()를 반드시 호출한다.
sizeof를 생략한다.
realloc()으로만 해제한다.
3
다음 중 다차원 배열을 포인터로 받는 함수 선언은?

void func(int **arr);
void func(int arr[][]);
void func(int arr[10][10]);
void func(*arr[10][10]);
void func(arr[][]);
3
다음 중 C 언어에서 함수 포인터 배열 선언으로 올바른 것은?

int (*funcArr[10])(int);
int *funcArr(int[10]);
int funcArr*(int[10]);
int funcArr(int*)[10];
int (*funcArr)(int)[10];
1
다음 중 C 언어에서 volatile 키워드의 역할은?

변수 값이 컴파일러 최적화 없이 자주 변경될 수 있음을 알림
변수를 상수로 만듦
변수를 전역으로 선언
변수를 정적으로 선언
변수를 자동으로 초기화
1
다음 중 C 언어에서 restrict 키워드의 의미는?

포인터가 가리키는 메모리가 중복되지 않음을 보장
포인터가 상수를 가리킴
포인터를 volatile로 선언
포인터를 동적으로 할당
포인터를 정적으로 할당
1
다음 중 C 언어에서 함수 포인터를 통해 콜백 함수 구현 시 올바른 설명은?

함수 포인터는 함수 주소를 저장한다
콜백 함수는 동적 할당이 필요하다
콜백 함수는 전역 함수만 가능하다
함수 포인터는 변수처럼 동작하지 않는다
콜백 함수는 반드시 void 반환형이어야 한다
1
다음 중 C 언어에서 union의 주요 특징은?

모든 멤버가 같은 메모리 공간을 공유한다
모든 멤버가 독립된 메모리 공간을 가진다
union은 구조체의 별칭이다
union은 상수를 저장한다
union은 포인터만 저장한다
1
다음 중 C 언어에서 올바른 메모리 누수 방지 방법은?

동적 할당 후 반드시 free() 호출
malloc() 대신 calloc() 사용
포인터를 NULL로 초기화
정적 변수 사용
전역 변수 최소화
1
다음 중 C 언어에서 함수 내부 static 변수의 특징은?

함수 호출 간 값이 유지된다
함수 밖에서는 접근 불가
초기화는 한 번만 수행
함수마다 별도의 변수 생성
위 모두 맞음
5
다음 중 C 언어에서 올바른 매크로 함수 정의로, 부작용을 최소화하는 것은?

#define SQUARE(x) ((x)*(x))
#define SQUARE(x) (x*x)
#define SQUARE(x) x*x
#define SQUARE(x) ((x) * (x)
#define SQUARE(x) ((x) * x)
1
다음 중 C 언어에서 inline 함수의 특징은?

컴파일러가 함수 호출 대신 함수 본문을 삽입할 수 있다
함수 포인터로 사용할 수 없다
inline은 반드시 헤더 파일에 정의해야 한다
inline 함수는 재귀 호출 불가능
inline 함수는 반드시 void 반환형이어야 한다
1
다음 중 C 언어에서 volatile 키워드를 사용하는 이유는?

하드웨어 레지스터 접근 시 컴파일러 최적화 방지
멀티스레딩에서 변수 동기화
포인터 타입 변환
메모리 동적 할당
배열 크기 지정
1
다음 중 C 언어에서 static 변수와 extern 변수의 차이는?

static은 파일 내에서만 유효, extern은 외부에서 참조 가능
static은 전역 변수, extern은 지역 변수
static 변수는 항상 초기화, extern 변수는 그렇지 않음
static 변수는 포인터, extern은 일반 변수
모두 틀림
1
다음 중 C 언어에서 void 포인터(void*)의 특징은?

모든 타입의 포인터로 변환 가능
역참조 불가능
포인터 산술 불가능
다형성 지원
위 모두 맞음
5
다음 중 C 언어에서 malloc() 실패 시 반환하는 값은?

NULL
0
-1
예외 발생
할당된 메모리 주소
1
다음 중 C 언어에서 올바른 동적 배열 구현 방법은?

포인터로 동적 메모리 할당 후 인덱스로 접근
정적 배열 사용
포인터 없이 배열 선언
전역 배열 사용
배열 크기 고정
1
다음 중 C 언어에서 올바른 메모리 덤프 확인 함수는?

memcpy()
memcmp()
memset()
malloc()
free()
2
다음 중 C 언어에서 올바른 함수 선언과 정의 매칭은?

int func(int a);
int func(int a) { return a+1; }
void func(void);
void func(void) { return; }
모두 맞음
5
다음 중 C 언어에서 올바른 다차원 배열 포인터 선언은?

int (*ptr)[10];
int *ptr[10];
int **ptr;
int ptr[10][10];
모두 맞음
4
다음 중 C 언어에서 올바른 union 사용 예는?

메모리 절약을 위해 여러 타입 중 하나만 저장할 때
다중 멤버 저장 시
함수 포인터 저장 시
상수 저장 시
모두 틀림
1
다음 중 C 언어에서 올바른 메모리 해제 시점은?

동적 할당 후 사용이 끝난 즉시
프로그램 종료 시
전역 변수 사용 시
함수 호출 시
무조건 main 함수에서
1
다음 중 C 언어에서 올바른 비트 연산자 사용 예는?

& (AND)
| (OR)
^ (XOR)
~ (NOT)
모두 맞음
5
다음 중 C 언어에서 올바른 포인터와 배열의 관계 설명은?

배열 이름은 배열 첫 요소의 주소로 해석된다
포인터는 변수 주소 저장
배열과 포인터는 다르다
포인터는 동적 메모리 할당에 주로 사용
위 모두 맞음
5
다음 중 C 언어에서 올바른 다중 포인터 선언과 활용 예는?

int **ptr;
int ***ptr;
포인터 배열
다중 포인터를 통한 2차원 배열 접근
위 모두 맞음
5
다음 중 C 언어에서 올바른 함수 인자 전달 방식은?

Call by Value
Call by Reference (포인터 사용)
모두 맞음
Call by Name
Call by Macro
3
다음 중 C 언어에서 올바른 메모리 초기화 함수는?

memset()
bzero()
calloc()
malloc()
free()
3
다음 중 C 언어에서 올바른 파일 입출력 함수는?

fopen()
fclose()
fread()
fwrite()
모두 맞음
5
다음 중 C 언어에서 올바른 동적 메모리 재할당 함수는?

realloc()
malloc()
free()
calloc()
memcpy()
1
다음 중 C 언어에서 올바른 메모리 접근 권한 지정 키워드는?

const
volatile
restrict
static
extern
1
다음 중 C 언어에서 올바른 함수 오버로딩 방법은?

C는 함수 오버로딩 지원하지 않음
함수 이름 변경으로 대체
매크로 사용
void 포인터 사용
위 모두 맞음
1
다음 중 C 언어에서 올바른 메모리 덤프 검사 함수는?

memcmp()
memcpy()
memset()
malloc()
free()
1
다음 중 C 언어에서 올바른 문자열 처리 함수는?

strcpy()
strlen()
strcmp()
strcat()
모두 맞음
5
다음 중 C 언어에서 올바른 전처리기 지시문은?

#define MAX 100
#include <stdio.h>
#undef MAX
#if defined(MAX)
모두 맞음
5
다음 중 C 언어에서 올바른 표준 입출력 함수는?

printf()
scanf()
fprintf()
fscanf()
모두 맞음
5
