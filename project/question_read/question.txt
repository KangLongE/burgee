다음 중 C 언어에서 정수를 입력받는 함수로 가장 적절한 것은?

printf()
scanf()
gets()
puts()
fopen()
2
다음 중 C 언어에서 올바른 변수 선언은 무엇인가?

int number;
num int;
int 123num;
int-num;
int;
1
다음 중 C 언어에서 포인터 변수 선언이 올바른 것은 무엇인가?

ptr int*;
int ptr*;
*int ptr;
int *ptr;
int* ptr();
4
다음 중 C 언어에서 for 반복문의 초기화식으로 올바른 것은?

for(i = 0; i < 10; i++) {}
for i = 0; i < 10; i++
for(i = 0 i < 10; i++)
for(i = 0; i < 10 i++)
for(i = 10; i >= 0; i++)
1
다음 중 C 언어에서 배열 선언이 올바른 것은?

int arr[5];
int arr();
int arr[5,5];
arr int[5];
int *arr(5);
1
다음 중 C 언어에서 함수 반환형으로 올바른 것은?

void func();
int func();
float func()
double func();
string func();
2
다음 중 C 언어에서 구조체 선언이 올바른 것은?

struct Point { int x; int y; };
struct Point ( int x; int y; );
struct Point { x int; y int; };
struct Point { int x, y };
struct Point { int x; int y }
1
다음 중 C 언어에서 올바른 if 조건식은?

if (x = 5) {}
if (x == 5) {}
if x == 5 {}
if (x => 5) {}
if x = 5 {}
2
다음 중 C 언어에서 malloc 함수의 역할은 무엇인가?

메모리 할당
메모리 해제
파일 열기
입력 받기
출력 하기
1
다음 중 C 언어에서 올바른 문자열 초기화는?

char str[5] = "hello";
char str[] = "hello";
char str[4] = "hello";
char str[6] = {'h','e','l','l','o'};
char str[] = {'h','e','l','l','o','!'};
2
다음 중 C 언어에서 배열과 포인터의 차이점으로 올바른 것은?

배열은 고정 크기, 포인터는 가변 크기
배열은 정적 메모리, 포인터는 동적 메모리만 사용
배열은 함수만 사용 가능, 포인터는 변수만 사용 가능
포인터는 크기를 지정할 수 없다
배열은 전역 변수만 선언 가능
1
다음 중 C 언어에서 switch 문에 올바른 break 위치는?

case 1: break;
case 1: printf("a");
case 2: break;
case 1: printf("a"); break;
case 1: printf("a"); break; default: break;
4
다음 중 C 언어에서 올바른 함수 호출은?

func();
func;
func(void)
func{}
func()
1
다음 중 C 언어에서 배열의 인덱스 범위로 올바른 것은?

0 ~ (크기-1)
1 ~ 크기
-1 ~ (크기-1)
0 ~ 크기
1 ~ (크기+1)
1
다음 중 C 언어에서 올바른 주석 문법은?

// 주석
/* 주석 */
# 주석
<!-- 주석 -->
\\ 주석
1
다음 중 C 언어에서 구조체 변수 선언으로 올바른 것은?

struct Point p;
Point p;
struct Point;
p Point;
struct Point p();
1
다음 중 C 언어에서 포인터 연산이 올바른 것은?

ptr++;
ptr--;
ptr = ptr + 1;
ptr = ptr - 1;
모두 맞음
5
다음 중 C 언어에서 함수 매개변수 선언이 올바른 것은?

void func(int a);
void func(a);
void func(int a, int);
func(int a, int b);
func(int a);
1
다음 중 C 언어에서 올바른 메모리 해제 함수는?

free();
malloc();
calloc();
realloc();
memset();
1
다음 중 C 언어에서 올바른 포인터 초기화는?

int *ptr = NULL;
int ptr = NULL;
int *ptr = 0;
int *ptr;
int *ptr = *ptr;
1
다음 중 C 언어에서 while 문이 실행되는 조건으로 올바른 것은?

조건식이 참일 때
조건식이 거짓일 때
항상 실행된다
조건이 없을 때
한 번만 실행된다
1
다음 중 C 언어에서 null 문자를 의미하는 것은?

'\0'
'0'
""
null
'\n'
1
다음 중 C 언어에서 전처리 지시어에 해당하는 것은?

#define
#include
#ifdef
#ifndef
모두 해당
5
다음 중 C 언어에서 파일 입출력 함수가 아닌 것은?

fopen()
fscanf()
fputs()
fclose()
printf()
5
다음 중 C 언어에서 정수를 저장할 수 있는 자료형은?

int
float
char
double
void
1
다음 중 C 언어에서 함수 포인터 선언으로 올바른 것은?

int (*fp)(int, int);
int *fp(int, int);
(*int fp)(int, int);
int (fp*)(int, int);
int fp(int, int);
1
다음 중 C 언어에서 문자 하나를 입력받는 함수는?

getchar()
scanf("%s", str);
puts(str);
fscanf(stdin, "%c", &ch);
gets(str);
1
다음 중 C 언어에서 메모리 동적 할당과 관련된 함수가 아닌 것은?

malloc()
calloc()
realloc()
free()
sizeof()
5
다음 중 C 언어에서 switch 문에서 사용 불가능한 자료형은?

int
char
enum
float
short
4
다음 중 C 언어에서 반복문에 해당하지 않는 것은?

for
while
do-while
repeat
없음
4
다음 중 C 언어에서 배열 이름이 의미하는 것은?

배열의 첫 번째 요소의 주소
배열 전체 값
배열 요소의 개수
배열의 크기
배열의 자료형
1
다음 중 C 언어에서 NULL 포인터의 용도는?

초기화되지 않은 포인터 표시
메모리 오류 발생
함수 종료
배열 끝 표시
문자열 끝 표시
1
다음 중 C 언어에서 문자열 길이를 반환하는 함수는?

strlen()
strcpy()
strcat()
strcmp()
sizeof()
1
다음 중 C 언어에서 const 키워드의 의미는?

상수로 선언되어 값 변경 불가
전역 변수 선언
메모리 초기화
포인터 선언
함수 호출
1
다음 중 C 언어에서 논리 연산자가 아닌 것은?

&&
||
!
& 
!=
4
다음 중 C 언어에서 정수 0으로 나눌 때 발생하는 오류는?

런타임 오류
컴파일 오류
링크 오류
논리 오류
문법 오류
1
다음 중 C 언어에서 전역 변수의 특징으로 올바른 것은?

모든 함수에서 접근 가능
해당 함수에서만 사용 가능
반드시 static으로 선언해야 함
함수 매개변수로만 사용 가능
메인 함수에서만 사용 가능
1
다음 중 C 언어에서 문자 ‘A’를 아스키 코드 값으로 저장하는 방법은?

char ch = 'A';
char ch = "A";
char ch = 65;
int ch = "A";
char ch = A;
1
다음 중 C 언어에서 파일을 쓰기 모드로 여는 함수는?

fopen("file.txt", "w");
fwrite("file.txt", "r");
fopen("file.txt", "a+");
open("file.txt", "w");
write("file.txt", "w");
1
다음 중 C 언어에서 typedef의 용도로 가장 적절한 것은?

자료형에 새로운 이름 정의
함수의 반환값 설정
포인터 초기화
함수 오버로딩
메모리 할당
1
다음 중 C 언어에서 구조체의 정의가 올바른 것은?

struct Point { int x; int y; };
Point struct { x; y; };
int struct Point(x, y);
struct { int x, y } Point;
define struct Point { int x, y; };
1
다음 중 C 언어에서 포인터와 배열의 공통점은?

이름이 주소를 나타냄
모두 함수이다
자료형을 필요로 하지 않음
크기를 변경할 수 있음
항상 전역 변수이다
1
다음 중 C 언어에서 문자열 복사 함수는?

strcpy()
strlen()
strcmp()
strcat()
strtok()
1
다음 중 C 언어에서 변수의 유효 범위를 제어하는 키워드는?

static
int
long
float
char
1
다음 중 C 언어에서 파일 끝을 확인하는 함수는?

feof()
ferror()
fclose()
fgetc()
rewind()
1
다음 중 C 언어에서 형 변환 연산자가 올바르게 사용된 것은?

(float)x
float x
(x)float
float(x, y)
convert(x)
1
다음 중 C 언어에서 조건 연산자에 해당하는 것은?

?:
&&
||
++
==
1
다음 중 C 언어에서 %d 형식 지정자의 용도는?

정수 출력
문자 출력
실수 출력
문자열 출력
16진수 출력
1
다음 중 C 언어에서 main 함수의 반환형으로 가장 일반적인 것은?

int
void
char
float
long
1
다음 중 C 언어에서 구조체 멤버에 접근하는 연산자는?

.
::
#
*
&
1
다음 중 C 언어에서 논리 참을 나타내는 값은?

0이 아닌 값
0
-1
1만 해당
NULL
1
다음 중 C 언어에서 헤더 파일을 포함하는 올바른 방법은?

#include <stdio.h>
import <stdio.h>
require <stdio.h>
add <stdio.h>
link <stdio.h>
1
다음 중 C 언어에서 switch 문 내에서 사용 가능한 키워드는?

case
when
default
break
모두 해당
5
다음 중 C 언어에서 포인터가 NULL인지 확인하는 방법으로 올바른 것은?

if (ptr == NULL)
if (ptr = NULL)
if (*ptr == 0)
if (&ptr == NULL)
if (NULL == *ptr)
1
다음 중 C 언어에서 재귀 함수의 특징은?

자기 자신을 호출한다
반드시 반복문을 포함한다
main 함수 안에서만 사용된다
포인터를 반드시 사용한다
실행 순서를 역순으로 바꾼다
1
다음 중 C 언어에서 가장 작은 메모리 크기를 가지는 자료형은?

char
int
float
double
long
1
다음 중 C 언어에서 break 문이 주로 사용되는 경우는?

반복문 탈출
조건문 실행
함수 종료
메모리 해제
포인터 초기화
1
다음 중 C 언어에서 배열의 크기를 컴파일 타임에 결정하려면 필요한 정보는?

배열의 크기 상수
배열의 이름
포인터
자료형
함수 포인터
1
다음 중 C 언어에서 실행 중에 오류가 발생하는 경우는?

0으로 나눌 때
정의되지 않은 변수 사용
세미콜론 빠짐
헤더 파일 누락
컴파일 명령 누락
1
다음 중 C 언어에서 표준 입력을 의미하는 것은?

stdin
stdout
stderr
stdfile
input
1
다음 중 C 언어에서 문자열 길이를 반환하는 함수는?

strlen()
strcpy()
strcat()
strchr()
strcmp()
1
다음 중 C 언어에서 컴파일 에러가 발생하는 경우는?

정의되지 않은 변수 사용
0으로 나누기
파일을 찾을 수 없음
메모리 누수
논리 오류
1
다음 중 C 언어에서 NULL 포인터의 의미는?

어떠한 주소도 가리키지 않음
주소 0xFFFFFFFF를 가리킴
문자열 끝을 나타냄
포인터 초기화 값
0이 아닌 주소
1
다음 중 C 언어에서 구조체 변수에 값을 대입하는 올바른 문법은?

p.x = 10;
p->x = 10;
*p.x = 10;
x.p = 10;
p:x = 10;
1
다음 중 C 언어에서 반복문이 아닌 것은?

do-while
while
for
goto
switch
5
다음 중 C 언어의 전처리기 지시문이 아닌 것은?

#include
#define
#typedef
#undef
#if
3
다음 중 C 언어에서 const 키워드의 역할은?

변경 불가능한 변수 선언
전역 변수 선언
정수형 선언
함수 선언
헤더 포함
1
다음 중 C 언어에서 배열 이름이 의미하는 것은?

배열의 첫 번째 요소의 주소
배열의 전체 값
배열의 크기
배열의 끝 주소
배열의 요소 수
1
다음 중 C 언어에서 포인터의 크기(바이트)는 무엇에 따라 결정되는가?

컴파일러 및 시스템 아키텍처
포인터가 가리키는 데이터 크기
배열의 크기
문자열 길이
변수 이름
1
다음 중 C 언어에서 문자열 리터럴을 잘못 사용한 것은?

char *str = "hello";
char str[] = "hello";
char str[6] = "hello";
char str[] = {'h','e','l','l','o','\0'};
char str[] = "hello\0";
5
다음 중 C 언어에서 EOF가 의미하는 것은?

End Of File
Enter Or Finish
Exit On Failure
Error Of Function
Empty Or Full
1
다음 중 C 언어에서 재귀 호출이 잘못 사용된 예는?

종료 조건 없이 자기 자신 호출
조건문 안에서 함수 호출
매개변수로 줄어드는 값 전달
void형 함수 사용
반환값을 저장하지 않음
1
다음 중 C 언어에서 double형 변수의 특징은?

소수점 포함 가능, 정수보다 정밀도 높음
문자만 저장 가능
메모리 1바이트 사용
주소 저장 가능
값의 변경 불가능
1
다음 중 C 언어에서 printf() 함수의 반환값은?

출력한 문자 수
에러 코드
0 또는 1
문자열 길이
파일 포인터
1
다음 중 C 언어에서 배열을 함수의 인자로 전달할 때 올바른 선언은?

void func(int arr[])
void func(arr)
void func[5](int)
int func(int arr)
char func(char)
1
다음 중 C 언어에서 포인터를 함수의 매개변수로 사용할 때의 장점은?

값 변경 가능
매개변수 수 감소
성능 향상
메모리 절약
모두 해당
5
다음 중 C 언어에서 main 함수의 실행 결과가 운영체제에 영향을 주기 위해 사용하는 것은?

return 문
exit 문
goto 문
break 문
continue 문
1
다음 중 C 언어에서 문자열 비교 함수는?

strcmp()
strchr()
strcpy()
strcat()
strlen()
1
다음 중 C 언어에서 포인터 선언이 올바른 것은?

int *ptr;
*int ptr;
ptr *int;
int ptr*;
int -> ptr;
1
다음 중 C 언어에서 배열의 마지막 인덱스를 구하는 공식으로 가장 적절한 것은?

크기 - 1
크기
크기 / 2
크기 * 2
항상 0
1
다음 중 C 언어에서 논리 연산자가 아닌 것은?

&&
||
!
&
^
4
다음 중 C 언어에서 변수에 저장된 값을 출력하는 함수는?

scanf()
gets()
puts()
fopen()
printf()
5
다음 중 C 언어에서 배열을 선언하는 올바른 문장은?

int arr[10];
int[10] arr;
arr int[10];
int arr[];
arr[10] int;
1
다음 중 C 언어에서 문자형 데이터를 저장할 수 있는 자료형은?

char
float
int
double
void
1
다음 중 C 언어에서 반복을 무한히 수행하도록 만든 while문은?

while(1)
while(0)
while(NULL)
while(x == y)
while(false)
1
다음 중 C 언어에서 포인터의 역참조 연산자는?

&
*
@
#
!
2
다음 중 C 언어에서 파일을 텍스트 쓰기 모드로 열기 위한 fopen 함수의 인자는?

"w"
"r"
"a"
"rb"
"wb"
1
다음 중 C 언어에서 함수의 반환형으로 올 수 없는 것은?

int
void
char
double
include
5
다음 중 C 언어에서 sizeof 연산자의 결과는?

바이트 단위 크기
비트 단위 크기
값의 주소
값의 실제 내용
함수 포인터
1
다음 중 C 언어에서 switch문에 사용할 수 없는 타입은?

int
char
float
enum
short
3
다음 중 C 언어에서 main 함수가 호출되는 횟수는?

1회
2회
0회
함수 수만큼
무한히
1
다음 중 C 언어에서 break 문이 사용되는 제어문은?

switch, for, while
for, if, else
while, if, switch
do, if, goto
continue, break, return
1
다음 중 C 언어에서 포인터 배열의 선언으로 올바른 것은?

int *arr[5];
int arr[*5];
int (*arr)5;
*int arr[5];
int arr[5*];
1
다음 중 C 언어에서 힙 영역에 메모리를 동적으로 할당하는 함수는?

malloc()
sizeof()
scanf()
free()
realloc()
1
다음 중 C 언어에서 구조체 멤버에 접근하기 위한 연산자는?

.
::
>
->
[]
5
다음 중 C 언어에서 문자열을 복사하는 함수는?

strcpy()
strcat()
strlen()
strcmp()
sprintf()
1
다음 중 C 언어에서 메모리를 해제하는 함수는?

free()
malloc()
alloc()
exit()
null()
1
다음 중 C 언어에서 포인터가 NULL인지 확인하는 올바른 조건문은?

if(ptr == NULL)
if(NULL = ptr)
if(ptr)
if(*ptr == NULL)
if(ptr = NULL)
1
다음 중 C 언어에서 void 포인터의 특징은?

어떤 타입이든 가리킬 수 있음
문자만 저장 가능
정수 전용
함수 포인터만 저장 가능
주소가 없음
1
다음 중 C 언어에서 함수 포인터 선언이 올바른 것은?

int (*fp)(int, int);
int *fp(int, int);
(*int) fp(int, int);
fp(*int)(int, int);
int (fp*)(int, int);
1
